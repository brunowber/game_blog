Pré-requisitos de utilização do GameBlog!
===========================================
   Criação de uma VirtualEnv e instalação do arquivo requirements.txt
   Utilizando o comando:

$ pip install -r requirements.txt
